[build]
  functions = "netlify/functions"   # pasta das Netlify Functions (backend)
  publish = "build"                 # pasta do build do React (frontend)

[functions]
  directory = "netlify/functions"   # redundante, mas reforça onde estão as funções
